{% extends 'base.html.twig' %}

{% block title %}New Items{% endblock %}

{% block body %}
    <h1>Create new Items</h1>
    <style>
        a:link, a:visited {
            background-color: darkblue;
            color: white;
            border: 2px black;
            padding: 10px 20px;
            text-align: center;
            text-decoration: none;
            display: inline-block;
            border-radius: 12px;
        }

        a:hover, a:active {
            background-color: black;
            color: lightskyblue;
        }
    </style>

    {{ include('productDetails/_form.html.twig') }}


    <a href="{{ path('app_productDetails_index') }}">back to list</a>
{% endblock %}
