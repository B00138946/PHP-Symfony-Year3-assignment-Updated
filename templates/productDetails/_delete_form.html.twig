<form method="post" action="{{ path('app_productDetails_delete', {'id': productDetails.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ productDetails.id) }}">
    <button class="btn">Delete</button>
</form>
